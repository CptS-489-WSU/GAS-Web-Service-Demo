//courseExists -- Given a course name, city, state, and country, this function searches the database to determine 
//if the course already exists in the database, returning true if so, false if not.
function courseExists(name, city, state, country) {
  //TO DO: Fill this in.
  
}
