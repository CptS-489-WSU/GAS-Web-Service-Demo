//generateCourseId-- Given the name, city, state and country of a course known not to already exist in the database, return
//a unique course id.
function generateCourseId(name, city, state, country) {
  //TO DO: Write code
}